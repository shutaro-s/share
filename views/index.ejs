<!DOCTYPE html>
<html lang="ja" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>トップページ</title>
  </head>
  <body>
    <h1><%= employees[0].name %></h1>
      <% if (employees[0].admin_flag == 1) { %>
        <a href="/employee/<%= employees[0].password %>">従業員一覧</a>
        <a href="/new/<%= employees[0].password %>">従業員追加</a>
        <a href="/confirm">全体シフト確認</a>
      <% } %>
    <a href="/pass/<%= employees[0].id%>">パスワード変更</a>
    <a href="/submit/<%= employees[0].id%>/<%= employees[0].password%>">シフト提出</a>
    <h1>希望</h1>
    <table width="40%">
      <tr>
        <th align="center">日付</th>
        <th align="center">希望開始時間</th>
        <th align="center">希望終了時間</th>
        <th align="center">備考</th>
        <th align="center"></th>
        <th align="center"></th>
      </tr>
      <% employees.forEach((employee)=>{ %>
      <tr>
        <td align="center"><%= employee.YEAR %>/<%= employee.MONTH %>/<%= employee.DAY %><% switch(employee.DAYOFWEEK){
          case 1: %>
            (日)
            <% break; %>
          <% case 2: %>
            (月)
            <% break; %>
          <% case 3: %>
            (火)
            <% break; %>
          <% case 4: %>
            (水)
            <% break; %>
          <% case 5: %>
            (木)
            <% break; %>
          <% case 6: %>
            (金)
            <% break; %>
          <% case 7: %>
            (土)
            <% break;} %></td>
        <td align="center"><%= employee.STARTTIME %></td>
        <td align="center"><%= employee.ENDTIME %></td>
        <td align="center"><%= employee.ex %></td>
        <td align="center"><a href="/edit/<%= employee.shift_id %>">編集</a></td>
        <td align="center">
          <form action="/delete/<%= employee.shift_id %>/<%= employees[0].id %>" method="post">
            <input type="submit" value="削除">
          </form>
        </td>
      </tr>
    <% }) %>
  </table>
  <br>
    <a href="/">ログアウト</a>
  </body>
</html>